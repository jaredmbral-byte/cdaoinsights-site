import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // No static export — Vercel handles Next.js natively
  // Keeps serverless functions available for future iterations
}

export default nextConfig
